{
  "$id": "NetworksMessage.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "A representation of an event",
  "type": "object",
  "unevaluatedProperties": false,
  "$comment": "TODO: oi4identifier needs to be updated and implemented here",
  "properties": {
    "correlationId": {
      "type": "string",
      "description": "Correlation ID used for logging and as debug information"
    },
    "DataSetClassId": {
      "type": "string",
      "format": "uuid",
      "description": "Dataset Class ID"
    },
    "MessageId": {
      "type": "string",
      "description": "Message ID",
      "$comment": "TODO: find a way to include the oi4Identifier better here",
      "pattern": "^[0-9]+-.+/[a-z0-9-.]+/.+/.+/.+$"
    },
    "MessageType": {
      "const": "ua-data",
      "description": "Message Type"
    },
    "PublisherId": {
      "description": "Publisher ID",
      "$comment": "TODO: find a way to include the oi4Identifier better here",
      "pattern:": "^.+/[a-z0-9-.]+/.+/.+/.+$"
    }
  },
  "required": [ "MessageId", "MessageType", "PublisherId" ]
}

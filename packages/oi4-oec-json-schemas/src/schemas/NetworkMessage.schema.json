{
  "$id": "NetworkMessage.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "OPC UA PubSub NetworkMessage representation. ADR 005: CorrelationId updated for OPC UA compliance.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "MessageId",
    "MessageType",
    "PublisherId",
    "Messages"
  ],
  "$comment": "ADR 005: CorrelationId is now optional and follows OPC UA Part 14 specification",
  "properties": {
    "CorrelationId": {
      "type": "string",
      "description": "ADR 005: Optional correlation identifier for request-response matching. Used to correlate response messages to their original request. Follows OPC UA Part 14-7.2.2.1 specification."
    },
    "DataSetClassId": {
      "type": "string",
      "description": "Dataset Class ID",
      "pattern": "^[0-9a-fA-F-]+$"
    },
    "MessageId": {
      "type": "string",
      "description": "Message ID",
      "$comment": "TODO: find a way to include the oi4Identifier better here",
      "pattern": "^.{1,}-(Registry|OTConnector|Utility|Persistence|Aggregation|OOCConnector|ITConnector)\\/(([a-z0-9-]+\\.)*([a-z0-9-]*)(?:\\/[^\\/`\\^\r\n]+){3})$"
    },
    "MessageType": {
      "const": "ua-data",
      "description": "Message Type"
    },
    "PublisherId": {
      "type": "string",
      "description": "Publisher ID",
      "$comment": "TODO: find a way to include the oi4Identifier better here",
      "pattern": "^(Registry|OTConnector|Utility|Persistence|Aggregation|OOCConnector|ITConnector)\\/(([a-z0-9-]+\\.)*([a-z0-9-]*)(?:\\/[^\\/`\\^\r\n]+){3})$"
    },
    "Messages": {
      "type": "array",
      "description": "Messages Array",
      "items": {
        "$ref": "DataSetMessage.schema.json"
      }
    }
  }
}